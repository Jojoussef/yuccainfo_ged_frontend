import { LoginForm } from '@/components/login-form';

export default function LoginPage() {
    return (
        <main className='animate-fade-in flex min-h-screen items-center justify-center'>
            <LoginForm />
        </main>
    );
}
